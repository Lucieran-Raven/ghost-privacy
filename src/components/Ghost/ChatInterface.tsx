/**
 * Backward-compatible entry point.
 *
 * Chat UI composition now lives in `ChatInterfaceShell` with subsystem hooks under
 * `src/components/Ghost/chat/*`.
 */
export { default } from './ChatInterfaceShell';
